# Core dependencies (lightweight requests-based scraper)
requests>=2.32.0
python-dotenv>=1.0.0
fastmcp>=2.0.0

# Playwright scraper dependencies (uncomment if using browser-based scraper)
# beautifulsoup4>=4.12.0  # (playwright scraper - HTML parsing)
# playwright>=1.49.0      # (playwright scraper - browser automation)

